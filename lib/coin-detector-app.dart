import 'package:coint_detector/routes/home.dart';
import 'package:flutter/material.dart';

class CoinDetectorApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
